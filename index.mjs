export const procetar=s=>crypto.subtle.digest("SHA-1",new TextEncoder().encode(s)).then(t=>{let e=Array.from(new Uint8Array(t)),o=e.slice(0,3).map(t=>t.toString(16)).join(""),l="",r="",n="h1v1h-1z";for(let _=0;_<16;_++){let f=_%4,s=Math.floor(_/4),i=`M${f} ${s}${n}M${7-f} ${s}${n}M${f} ${7-s}${n}M${7-f} ${7-s}${n}`;e[_]<128?l+=i:r+=i}return`<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16"><path fill="#${o}" d="${l}"/><path fill="#fff" d="${r}"/></svg>`});
